# agent_swarm

多 agent 协作中枢：agents 把各自的 opencode 工作区注册到中央服务端，彼此可以求助。

## 结构

- `server/` — FastAPI 单服务：管理 REST API（JWT）、MCP 端点 `/mcp/`（ApiKey 鉴权）、插件分发 `/download/*`，SQLite 存储（`data/agent_swarm.db`）
- `plugin/` — opencode 插件（TS）：注册工作区、心跳、领取并执行求助任务，注入 swarm_* 工具
- `web/` — React 19 + Vite 管理界面（opencode.ai 风格）：接入/工作区/求助记录三个页面，dev :8701 代理到 :8700
- `deploy/` — 启停与安装脚本（sh + ps1）；`scripts/` — e2e 脚手架

PURPOSE: 明白了——你反复贴的是我 `tui.ts` 里现在用的提示词。对照 master 分支的 server 插件（`plugin/commands/swarm-register.md`）：它的提示词是生成 **PURPOSE + CAPABILITIES 两行（各≤80字）** 写进 `.agent-swarm.md`，再注册；而我现在的实现只生产一句 purpose，既没生成 capabilities、也没写回文件两行。这才是你让我"去看 master"的点。

改：headless 总结改为输出两行，解析后同时写 `.agent-swarm.md` 的 `PURPOSE:`/`CAPABILITIES:`，并上传服务器（服务端补 capabilities 支持）。
CAPABILITIES: 开发 FastAPI 服务端、opencode 插件（TS）、React 前端；调试 MCP、求助流程与部署脚本
WORKSPACE_ID: anKN3nc88cJnVND3cnjcmn
